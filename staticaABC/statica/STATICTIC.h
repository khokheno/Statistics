#include <iostream>
#include <vector>
#include<string.h>

using std::vector;

class STATICTIC{

public:
	int count(vector<int>a);
	int count(vector<double>a);
	int STATICTIC::count(vector<vector<char>>arr);
	double sum(vector<double>b);
	int sum(vector<int>b);
	double median(vector<double>a);
	double median(vector<int>a);
	vector<char> median(vector<vector<char>>str);
	double STD(vector<double> arr);
	double STD(vector<int> arr);
	int max(vector<int>arr);
	double max(vector<double>arr);
	vector<char> max(vector<vector<char>>str);
	int min(vector<int>arr);
	double min(vector<double>arr);
	vector<char> min(vector<vector<char>>str);



	












};